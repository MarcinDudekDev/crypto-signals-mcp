services:
  - type: web
    runtime: python
    name: crypto-signals-mcp
    plan: free
    buildCommand: pip install -r requirements.txt
    startCommand: python server.py --transport sse --port $PORT
    envVars:
      - key: PORT
        value: 10000
